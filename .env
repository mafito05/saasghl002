# --- Base de Datos del SaaS ---
# Conexión a la base de datos PostgreSQL que definimos en docker-compose.yml
# Asegúrate de que el usuario, contraseña y nombre de la BD coincidan.
DATABASE_URL="postgresql://saas_user:saas_password@postgres:5432/saas_db"

# --- Seguridad de la Aplicación (JWT) ---
# Clave secreta para firmar los tokens de autenticación de usuarios.
# ¡IMPORTANTE! Cámbiala por una clave compleja y segura en producción.
SECRET_KEY="e8f4b0b1e4c3d3a2b1a0c9d8e7f6a5b4c3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8"
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Clave de Encriptación de Campos ---
# ¡MUY IMPORTANTE! Clave usada para encriptar las API Keys de GoHighLevel en la base de datos.
# DEBE ser de 32 bytes. No la pierdas o no podrás desencriptar las claves guardadas.
FIELD_ENCRYPTION_KEY="T2l-58K_g9u_4sA92e-r_sEcReT_kEy_32bYt"

# --- Configuración de Docker ---
# Nombre de la red de Docker que usan los servicios (definido en docker-compose.yml)
DOCKER_NETWORK=my_saas_network
